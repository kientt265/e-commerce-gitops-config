apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  labels:
    app: product-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      nodeSelector:
        workload_type: stateless
        service_type: api-based
      containers:
      - name: product-service
        image: 582719176716.dkr.ecr.ap-southeast-1.amazonaws.com/product-service:latest
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "100m"
        ports:
        - containerPort: 3001
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: product-db-url
        - name: PRODUCT_PORT
          value: "3001"
        - name: KAFKA_BROKER_1
          value: "kafka:9092"
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
spec:
  selector:
    app: product-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
