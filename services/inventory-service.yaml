apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  labels:
    app: inventory-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      nodeSelector:
        workload_type: stateless
        service_type: event-driven
      containers:
      - name: inventory-service
        image: 582719176716.dkr.ecr.ap-southeast-1.amazonaws.com/inventory-service:refresh-code-1
        resources:
          requests:
            memory: "128Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "100m"
        ports:
        - containerPort: 3001
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secrets
              key: inventory-db-url
        - name: INVENTORY_PORT
          value: "3001"
        - name: KAFKA_BROKER_1
          value: "kafka:9092"
        - name: REDIS_HOST
          value: "inventory-redis"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: inventory-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: ClusterIP
